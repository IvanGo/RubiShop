<%= form_for(@bowtype) do |f| %>
  <% if @bowtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bowtype.errors.count, "error") %> prohibited this bowtype from being saved:</h2>

      <ul>
      <% @bowtype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
