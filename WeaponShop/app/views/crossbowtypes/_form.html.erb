<%= form_for(@crossbowtype) do |f| %>
  <% if @crossbowtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crossbowtype.errors.count, "error") %> prohibited this crossbowtype from being saved:</h2>

      <ul>
      <% @crossbowtype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
